spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

application:
  security:
    jwt:
      secret-key: 351b9e42a97ace5940d38f34a2dff6ba02f0833e5858db44cc3d3333a6c7efb1
      expiration: 86400000 #a day
      refresh-token:
        expiration: 604800000 #7 days

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://10.92.96.3:5432/gofly
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5432/gofly
    username: ${DB_USERNAME}
    password: ${DB_USERNAME}

---
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    database-platform: postgresql